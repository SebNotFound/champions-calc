<resources>
    <!-- Plain Material theme (no AppCompat dependency needed): the launcher screen
         is a simple programmatic layout, and the overlay draws its own chrome. -->
    <style name="Theme.ExoOverlay" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#080b12</item>
        <item name="android:windowBackground">#080b12</item>
    </style>
</resources>
